// cypress/support/e2e.ts

// Import custom commands (if any)
// import './commands';

// Cypress configuration or global setup can go here
beforeEach(() => {
  // Example: Reset cookies before each test
  cy.clearCookies();
});
